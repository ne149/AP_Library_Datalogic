<Window x:Class="GUI_Library.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Background="{DynamicResource MaterialDesignPaper}"
        Title="Reset Password" Height="220" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeaderText"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,16"
                   TextWrapping="Wrap"/>

        <PasswordBox Grid.Row="1" x:Name="PwBox"
                     materialDesign:HintAssist.Hint="New password"
                     Style="{StaticResource MaterialDesignOutlinedRevealPasswordBox}"
                     Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" x:Name="Status" Text="" FontSize="11"
                   Opacity="0.8" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="CANCEL" Click="Cancel_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Width="100" Margin="0,0,8,0"/>
            <Button Content="OK" Click="Ok_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>
